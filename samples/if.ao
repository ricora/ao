fn is_gt_0(x: i32) -> i32 {
  var result: i32 = 0;
  if x > 0 {
    result = 1;
  } else {
    result = 0;
  }
  result
}

fn main() -> i32 {
  let x: i32 = 1;
  let result: i32 = is_gt_0(x);
  print_int(result);
  0
}
