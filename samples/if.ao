fn is_gt_0(x: i32) -> bool {
  var result: bool = false;
  if x > 0 {
    result = true;
  } else {
    result = false;
  }
  result
}

fn main() -> i32 {
  let x: i32 = 100;

  let result: bool = is_gt_0(x);
  if result {
    print_int(1);
  } else {
    print_int(0);
  }

  0
}
